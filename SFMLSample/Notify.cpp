#include "pch.h"

/*int errorNotification(unsigned indexError) //
{
	switch (indexError)
	{
		case 1:
		{
			int msgboxID = MessageBox(
				NULL,
				(LPCWSTR)L"Podano nieprawid³owy indeks warstwy!",
				(LPCWSTR)L"EMIRIUSU ENGINE",
				MB_ICONEXCLAMATION | MB_OK | MB_DEFBUTTON2);

			return msgboxID;
			break;
		}
		case 2:
		{
			int msgboxID = MessageBox(
				NULL,
				(LPCWSTR)L"Nie uda³o siê odczytaæ tekstury!\nZastêpuje tekstur¹ tymczasow¹.",
				(LPCWSTR)L"EMIRIUSU ENGINE",
				MB_ICONEXCLAMATION | MB_OK | MB_DEFBUTTON2);

			return msgboxID;
			break;
		}
		case 3:
		{
			int msgboxID = MessageBox(
				NULL,
				(LPCWSTR)L"Nieprawid³owy typ tile'a!",
				(LPCWSTR)L"EMIRIUSU ENGINE",
				MB_ICONEXCLAMATION | MB_OK | MB_DEFBUTTON2);

			return msgboxID;
			break;
		}
		case 4:
		{
			int msgboxID = MessageBox(
				NULL,
				(LPCWSTR)L"Nie uda³o siê za³adowaæ pliku textureInfo.txt!",
				(LPCWSTR)L"EMIRIUSU ENGINE",
				MB_ICONEXCLAMATION | MB_OK | MB_DEFBUTTON2);

			return msgboxID;
			break;
		}
		case 5:
		{
			int msgboxID = MessageBox(
				NULL,
				(LPCWSTR)L"Nie uda³o siê za³adowaæ pliku biomu!",
				(LPCWSTR)L"EMIRIUSU ENGINE",
				MB_ICONEXCLAMATION | MB_OK | MB_DEFBUTTON2);

			return msgboxID;
			break;
		}
		case 6:
		{
			int msgboxID = MessageBox(
				NULL,
				(LPCWSTR)L"Nie uda³o siê za³adowaæ pliku biomes/info.txt",
				(LPCWSTR)L"EMIRIUSU ENGINE",
				MB_ICONEXCLAMATION | MB_OK | MB_DEFBUTTON2);

			return msgboxID;
			break;
		}
		case 7:
		{
			int msgboxID = MessageBox(
				NULL,
				(LPCWSTR)L"Nie uda³o siê stworzyæ super tekstury!",
				(LPCWSTR)L"EMIRIUSU ENGINE",
				MB_ICONEXCLAMATION | MB_OK | MB_DEFBUTTON2);

			return msgboxID;
			break;
		}
		default:
		{
			int msgboxID = MessageBox(
				NULL,
				(LPCWSTR)L"Wszystko OK!",
				(LPCWSTR)L"EMIRIUSU ENGINE",
				MB_ICONEXCLAMATION | MB_OK | MB_DEFBUTTON2);

			return msgboxID;
			break;
		}
	}
}*/


